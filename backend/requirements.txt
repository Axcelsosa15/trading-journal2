# Trading Journal backend dependencies (slim, deploy-ready).
# Web framework + server
fastapi>=0.110,<1.0
uvicorn>=0.25,<1.0
python-multipart>=0.0.9        # UploadFile / CSV import
starlette>=0.37               # CORS middleware (pulled by fastapi too)

# Data validation
pydantic>=2.6,<3.0
email-validator>=2.1           # pydantic EmailStr

# MongoDB
motor>=3.3,<4.0
pymongo>=4.6,<5.0
dnspython>=2.6                 # required for mongodb+srv:// (Atlas) URLs

# Auth & crypto
bcrypt>=4.1,<5.0
PyJWT>=2.8,<3.0
cryptography>=42.0            # Fernet encryption for broker secrets

# Config
python-dotenv>=1.0

# HTTP client for broker integrations
requests>=2.31

# AI insights
anthropic>=0.40
